[pytest]
testpaths = tests
addopts = -q -p no:cacheprovider
markers =
    integration_mysql: pruebas opt-in contra MySQL real
    integration_vector: pruebas opt-in contra motor vectorial real
filterwarnings =
    ignore:urllib3.*chardet.*charset_normalizer.*:Warning
